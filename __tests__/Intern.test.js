const getSchool = require("../lib/Intern");

test("checks if school is truthy", () => {
    expect(getSchool).toBeTruthy();
});

const getRole = require("../lib/Intern");

test("checks if role is truthy", () => {
    expect(getRole).toBeTruthy();
});